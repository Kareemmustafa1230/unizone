import 'package:flutter/material.dart';

class TaskAlt extends StatelessWidget {
  static const String routeName = 'Task-Alt';

  const TaskAlt({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple,
    );
  }
}
